import { InstructionsPipe } from "./instructions.pipe";

describe('InstructionsPipe', () => {
  it('create an instance', () => {
    const pipe = new InstructionsPipe();
    expect(pipe).toBeTruthy();
  });
});
